# 16강 디버깅 시각화 테스트

-------------

ros2 launch turtlebot3_bringup rviz2.launch.py //rviz2실행
ros2 launch turtlebot3_gazebo turtlebot3_world.launch.py //empty world말고 turtlebot world 실행
ros2 run turtlebot3_teleop teleop_keyboard //로봇을 조종할 수 있는 가상 키보드 실행

gazebo를 먼저 띄운후 로봇을 spawn하면 rviz를 실행했을때 가상의 공간에서 gazebo와 연동되어 로봇의 위치를 실시간으로 확인할 수 있다.
tele_op를 이용하여 로봇을 gazebo환경에서 움직이면 rviz2에서도 실시간으로 odometry가 찍히는것을 확인할 수 있다.

말그대로 실행되고 있는 topic을 하나씩 visualize하는것이라고 보면 된다. 시뮬레이션 토픽을 실행시킬 수 있지만 시각화를 하지 못한다면 소용이 없고, 
로봇의 파라미터를 변경했다고 해도 이것을 시각화 할 수 없으면 소용이 없기 때문에 이 visualize하는 과정은 굉장히 중요하다.

visualize 하는 방법은 크게 6가지이다.
-rqt_graph
-RViz2
-rqt_plot
-ros2 CLI
-rosbag2
-rqt_console

# CLI명령
ros2 topic list / echo / hz / info
ros2 node list / info
ros2 topic pub ...
rqt_graph

# RViz2
RViz2는 ROS2의 대표적인 visualize 도구이다. 숫자로만 이루어진 토픽을 가상 공간속 형상으로 보여줍니다.
센서 데이터, TF좌표계, 로봇모델, 경로또는 마커등을 표시해줄 수 있다.

흔히 발생한다고 하는 오류!
계산결과를 메시지에 담아 토픽으로 발행하고, RViz2에서 이것을 확인하려고 하면 안뜨는 경우가 있다고 한다. 이때 원인은 대개 RViz2내에서 scale, alpha값이 0이거나, Fixed Frame 불일치라고 한다.
그러니 안뜬다면 최우선적으로 확인해보기로 하자.

# rosbag2
로봇을 디버깅할때 가장 어려운 것은 어떠한 문제가 발생했을때 그것을 다시 정확하게 재현하기가 어렵다는 것이다. 
생각해보자. 자율주행 로봇을 만들다가 테스트 운전을 했는데 경로를 벗어나 오류가 생겼다면 그 경로를 완벽하게 다시 재현하기는 어려울것이라고 생각한다. 
그 오류를 꼭 해결해야 한다면 그 오류를 다시 만들기 위해 몇시간 동안이나 계속 로봇을 굴려야 할지도 모른다.

하지만 그러한 문제점을 해결하기 위해 ROS2에는 좋은 기능이 존재한다고 한다.
바로 rosbag2인데 간단하게 말하자면 로봇이 갔던 모든 경로를 메시지와 타임스탬프와 같이 저장하여 언제든 그 문제가 생겼던 상황을 다시 재현할 수 있다는 것이다.
