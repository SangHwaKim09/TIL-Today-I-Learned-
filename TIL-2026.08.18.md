# 17강. 벡터, 행렬 연산과 내적, 외적

> ## 학습목표
1. 로봇의 모든 좌표계(월드, 베이스, 센서)가 결국 하나의 기저(basis) 선택임을 이해합니다.
2. 행렬 곱이 어떤 조건에서 성립하는지 선형결합의 관점에서 설명할 수 있습니다.
3. 전치, 대칭, 반대칭 행렬을 구분하고, 외적이 반대칭행렬의 곱입을 유도하고 설명할 수 있습니다.
4. 내적의 기하학적 의미(투영, 각도)를 이해하고, 정사영을 계산할 수 있습니다.
5. 외적의 기하학적 의미(법선, 회전축, 넓이)를 이해하고, 평면의 법선 벡터를 계산할 수 있습니다.
6. 선형독립, 기저, 차원, rank를 판별하고, 해의 존재성 판정에 활용할 수 있습니다.

> ## 들어가며
지난 16강 동안의 과정에서 로봇 소프트웨어의 기본이라고 할 수 있는 리눅스 터미널, Git협업환경, Python 프로그래밍, ROS2통신에 대해서 배웠다.
로봇은 혼자 움직일 수 없다. 끊임없이 자신의 위치를 추정하며 다음 명령을 수행하기 위해 통신을 해야한다.
이때 발생하는 여러가지 질문들이 있다.
* 카메라가 인식한 물체는 로봇을 기준으로 어느 위치에 있는가?
* 지금 자세에서 목표한 자세로 가려면 어떻게 회전해야 하는가?
* 두 센서가 각자 측정한 좌표를 하나의 좌표로 합치려면?

이 질문들의 답은 결국 하나의 행렬 곱으로 귀결된다. 이 행렬 곱을 수행하기 위한 가장 작은 요소인 벡터, 행렬, 내적, 외적에 대해서 자세히 알아본다.

> ## 벡터(vector)
벡터(vector)는 크기와 방향을 함께 갖는 물리량이다. 로봇을 제작할때 고려할 수 있는 힘, 속도, 가속도, 위치, 회전축등이 모두 벡터이다.
벡터는 행렬의 꼴로 나타낼 수 있는데, 만약 1차원의 벡터라면 총 n개의 성분을 세로로 배열해 나타낼 수 있다.
마찬가지로 차원이 n개로 늘어날 경우 행 또한 n개로 늘어나도록 표현할 수 있다.

* 벡터공간(vector space) : 덧셈과 스칼라배에 대해 닫혀 있는 벡터들의 집합. 쉽게말해 이 공간안의 한 벡터에 다른 벡터를 더하거나 실수배해도 여전히 집합안에 속할 경우 성립한다.
* 기저(basis) : 공간을 표현하는 최소한의 독립 방향 집합. 3D공간을 예를 들어 설명하자면 x, y, z 총 세개의 축으로 이루어져 있는데, 이 세개의 축이 표준기저라고 할 수 있다.
* 차원(dimension) : 기저 벡터의 개수. 바로 윗절에서 설명한 기저는 공간을 이루는 축의 개수라고 했는데, 이 축들도 엄밀히 따지면 벡터이기 때문에 이 벡터의 개수라고 보면 된다.

이것이 도대체 로봇 개발할때 어떻게 쓰이냐?
로봇의 base좌표계, camera가 사용하는 좌표계, LiDAR센서의 좌표계는 모두 설치되는 위치가 다를것이다.
설치위치가 달라진다는 것은? 곧 기준좌표가 모두 달라진다는것을 의미한다. 그러므로 모두 다른 기저를 선택한다고 이해해야 한다.
벡터는 대학교때부터 굉장히 오랫동안 접해온 개념이지만 볼때마다 받아들이는 느낌이 달라진다. 아마 이해의 깊이가 점점 더 깊어지기 때문이지 않을까.

> ## 행렬(matrix)
행렬은 수를 직사각형의 모양으로 배열한 것이다. 사실, 여러개의 식을 하나의 구조에 집어넣은 것이라고 생각하면 편하다.
그렇다면 왜 행렬이라는 개념을 사용하는 걸까? 
복잡한 수학문제나 물리현상을 식으로 정리하다 보면 변수도 여러개에 식도 여러개가 나온다. 이때 행렬을 사용하면 식 여러개를 한번에 다룰 수 있게된다.
약간 추상적인데, 벡터공간 안에 속해있는 벡터들은 선형변환 함수라고 할 수 있다. 이 함수들또한 매우 추상적인 상태이기 때문에 기저와 이 함수를 함께 계산하면 상태가 결정되기 때문에 행렬이 필요한것이다.

행렬을 구했다면 이 행렬들을 곱하는 행렬곱을 구해야 하는데 이때에는 규칙이 있다. 같은자리의 성분별로 곱하는 것이 아니라 행 X 열의 규칙을 따른다.

그 이유는 간단하다. 행렬-벡터 곱은 열벡터의 선형결합이기 때문이다.

